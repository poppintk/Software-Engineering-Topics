Prefix Sum is a optimaization technique when dealing with array of Integers.
Especially in questions liek sub array sum or continous subarray

template1:
 int[] sum = new int[nums.length]
 sum[0] = nums[0]
 for (int i = 1; i < nums.length; i++) {
    sum[i] = sum[i - 1] + nums[i];
 }
 
 template2:
 int[] sum = new int[N + 1];
 for (int i = 0; i < N; i++) {
    sum[i + 1] = sum[i] + A[i];
 }
 
 
 
